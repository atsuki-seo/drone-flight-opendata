---
paths:
  - "converter/analyzers/**"
---

# ホバリング判定基準

- `speed < 0.3 m/s` が **5秒以上連続** した区間をホバリングと判定する
- 代表点は区間内の緯度経度の中央値を使用する
- 閾値は `converter/config.py` で一元管理する
- SRTデータが利用可能な場合、ホバリング中のカメラ方位角（yaw）を付加する
